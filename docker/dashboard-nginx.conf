# Dashboard static serving (ADR-017): SPA fallback, no caching surprises on
# index.html, immutable hashed assets. API/WS routing happens at the Caddy
# edge, not here.
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location /assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }

  location / {
    add_header Cache-Control "no-cache";
    try_files $uri /index.html;
  }
}
