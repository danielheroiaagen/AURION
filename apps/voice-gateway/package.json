{
  "name": "@aurion/voice-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "AURION realtime voice gateway (ADR-018).",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "dev": "tsc -p tsconfig.build.json --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@types/ws": "^8.5.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
